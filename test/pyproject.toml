[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "brad", email = "bradleeborgholth@u.boisestate.edu" }
]
requires-python = ">=3.14"
dependencies = [
    "pyledger",
]



[project.scripts]
test = "test:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
